git clone git@github.com:UCR-CEE/cee-200-homework-1-numaer99.git

mkdir src
git add src/
git commit -m "src file is created"

git add img.png/
git commit -m "image is attached"

mkdir command
git add command/
git commit -m "all lines are here"

git push origin main 
